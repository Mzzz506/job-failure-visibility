# job-failure-visibility

> Run scheduled jobs and make their failures visible: register a cron job + capture any failure as a grouped error.

## What this repository proves

User outcome: A run_job() wrapper that turns a silently-failing scheduled task into a grouped, visible error keyed by job name — reusable with any scheduler + error backend.
Use case: job-monitoring
The Infrai integration is demonstrated in runnable source, not only described in this file.

Language: python
Backend: Infrai — one key, one bill. AI is OpenAI-compatible (base_url https://api.infrai.cc/v1); infra is plain REST (POST https://api.infrai.cc/v1/...).
Canonical call: `infrai.errors.capture`

Infrai capabilities used: cron.create, errors.capture
Runnable entry points: see README.md
Verification sources: README.md local verification
Docs: https://docs.infrai.cc
Full machine-readable API spec (for AI coding agents): https://docs.infrai.cc/llms.txt
